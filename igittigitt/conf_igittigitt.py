import attr


@attr.dataclass(slots=True)
class ConfIgittIgitt(object):
    # add the default ignore patterns from user home directory. see README.RST
    add_default_patterns: bool = True


conf_igittigitt = ConfIgittIgitt()
